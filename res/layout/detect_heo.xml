<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DashboardActivity" >

    <EditText
        android:id="@+id/heoid_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/textView1"
        android:ems="10"
        android:editable="false"
        android:hint="ID heo"
        android:inputType="none" />

    <EditText
        android:id="@+id/heoweight_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/heoid_text"
        android:layout_below="@+id/heoid_text"
        android:layout_marginTop="15dp"
        android:ems="10"
        android:hint="Weight" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/submit_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/heoweight_text"
        android:layout_below="@+id/heoweight_text"
        android:layout_marginTop="18dp"
        android:onClick="detectIDheo"
        android:text="Submit" />

</RelativeLayout>